import { Setoid, Ord, Semigroup, Monoid } from "../abstract-types";

export const equals = (a, b) => {};

export const lte = (a, b) => {};

export const concat = (a, b) => {};

export const empty = (M: StringConstructor) => "";
